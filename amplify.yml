version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - echo "Pulling Docker image"
            - docker pull antonyjm462/edu-platform
        build:
          commands:
            - echo "Running Docker container"
            - docker run -d -p 5000:5000 docker pull antonyjm462/edu-platform
    appRoot: /